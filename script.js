const cardsForm = document.getElementById("cards-form");

const displayCards = function(event) {
    event.preventdefault();

    
}

const rollCards = function(cardNumber, inverted) {
    
}

cardsForm.onsubmit = displayCards;